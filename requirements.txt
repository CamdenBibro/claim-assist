# Core dependencies
pandas>=2.0.0
requests>=2.31.0

# Web scraping dependencies  
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Local inference backends (optional - install based on your choice)
# For llama.cpp (recommended for local inference, especially AMD GPUs)
# No additional packages required - uses requests to communicate with llama.cpp server
# See LLAMACPP_AMD_SETUP.md for setup instructions

# For Transformers (CPU/GPU local inference)
torch>=2.0.0
transformers>=4.30.0

# Legacy cloud API support (optional - for backward compatibility)
anthropic>=0.40.0

# Development dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
